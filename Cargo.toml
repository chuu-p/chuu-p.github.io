[package]
name = "blog"
version = "0.1.0"
edition = "2021"

[dependencies]
color-eyre = "*"          # error handling
handlebars = "6.3.1"      # templating
maud = "0.26.0"           # html macro
pulldown-cmark = "0.11.0" # markdown to html
toml = "0.8.20"           # toml parser

[dev-dependencies]
filetime = "0.2.25"
tempfile = "3.18.0"
